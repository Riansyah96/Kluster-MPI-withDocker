# MPI Cluster dengan Docker

Cluster MPI sederhana dengan 3 node menggunakan Docker.

## Quick Start

```bash
# Beri permission executable
chmod +x start-cluster.sh scripts/setup-cluster.sh

# Jalankan setup otomatis
./start-cluster.sh
